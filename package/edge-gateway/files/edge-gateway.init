#!/bin/sh /etc/rc.common

START=95
STOP=10
USE_PROCD=1

start_service() {
	procd_open_instance
	procd_set_param command /usr/bin/edge_gateway /etc/edge-gateway/edge-gateway.conf
	procd_set_param respawn 5 10 3
	procd_set_param stdout 1
	procd_set_param stderr 1
	procd_close_instance
}
